<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" /> <!-- Define o fundo transparente -->
    <corners android:radius="12dp" /> <!-- Define o raio dos cantos para arredondar -->
    <stroke
        android:width="2dp"
    android:color="@color/black" />
</shape>